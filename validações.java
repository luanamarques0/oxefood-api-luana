public class validações {
    // @Min(value) -> Valor mínimo (inclusive).

    // @Max(value) ->	Valor máximo (inclusive).

    // @Positive ->	Apenas valores positivos (> 0).

    // @PositiveOrZero ->	Apenas valores ≥ 0.

    // @Negative ->	Apenas valores negativos (< 0).

    // @NegativeOrZero ->	Apenas valores ≤ 0.

    // @Digits(int, int) ->	Número com limites de dígitos inteiros e decimais.

    //-------------------------------- DATA --------------------------------

    // @Past ->	A data deve ser anterior à data atual.

    // @PastOrPresent ->	A data deve ser hoje ou no passado.

    // @Future ->	A data deve ser no futuro.

    // @FutureOrPresent	-> A data deve ser hoje ou no futuro.
}
// Dicas -> se precisar guardar as auterações, crie outra classe,
// coloque o usuario, uma coluna com o antes e o depois, essa classe é inserida no momento da criação